[project]
name = "torcida"
version = "0.1.0"
description = "Live sports pools with odds-priced picks and an AI radio narrator"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
asyncio_mode = "auto"
